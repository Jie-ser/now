# GeoReward: DA3 Geometry Reward + Best-of-N for Wan2.2 I2V
# Python >= 3.10 recommended
#
# Install from the repository root with:
#   python -m pip install -r requirements.txt
#
# PyTorch wheels are CUDA- and platform-specific. Install a compatible
# torch/torchvision/torchaudio build first from the official PyTorch index,
# then install this file from the normal Python package index.

# ============ Core ============
torch>=2.4.0
torchvision>=0.19.0
torchaudio
numpy>=1.23.5,<2
pillow
tqdm
# Used by DA3 model loading and by the `hf download` weight-download command.
huggingface_hub[cli]

# ============ Wan2.2 I2V ============
opencv-python>=4.9.0.80
diffusers>=0.31.0
transformers>=4.49.0,<=4.51.3
tokenizers>=0.20.3
accelerate>=1.1.1
imageio[ffmpeg]
imageio-ffmpeg
easydict
ftfy
dashscope

# Wan imports S2V and Animate modules from wan/__init__.py even when this
# project only uses I2V, so these are import-time dependencies.
decord
librosa
peft>=0.17.0

# xformers currently validates this FlashAttention API range at import time.
# The package is compiled against the installed PyTorch/CUDA toolchain.
flash-attn>=2.7.1,<2.8.0

# ============ Local Depth Anything 3 package ============
# Its pyproject.toml declares DA3 runtime dependencies such as xformers,
# open3d, pycolmap, e3nn, trimesh, omegaconf and safetensors.
-e ./Depth-Anything-3-main

# Install the local Wan package as well, rather than relying on PYTHONPATH.
-e ./Wan2.2

# ============ Optional, not required by GeoReward I2V ============
# DA3 Gaussian-splatting export: pip install 'Depth-Anything-3-main[gs]'
# DA3 web app:                  pip install 'Depth-Anything-3-main[app]'
# Wan Speech-to-Video extras:   pip install -r Wan2.2/requirements_s2v.txt
